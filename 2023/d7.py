import re
from typing import Iterable, Iterator

from main import fetch_lines


def first(lines: Iterable[str]) -> int:
    res = 0
    h = {}

    for line in lines:
        pass
    return res


def second(lines: Iterable[str]) -> int:
    res = 0

    for line in lines:
        pass
    return res


print(first(fetch_lines('d7.txt')))
print(second(fetch_lines('d7.txt')))
